Approval queue is not showing data directly created and approved by admins. 


In Audit Entry Details modal, Primary member is showing received by value. The actual received by row is missing. Fix both. Primary members details, phone, address, and submember list should be visible in the audit entry details. For sponsorship entries similar all input by user details should be visible. Remove the stored snapshot section.

In the Transaction Details modal, reflect the same membership details for any membership related transactions. For sponsorship transaction same spons details with all user input should be visible.

In the activity log modal, reflect the same structure for any financial activities. Make all these three modals look similar, with same data. 



In transaction details modal is cash management, show details of members/sponsors who paid the money. data available in the add transaction modal. for membership fees infer the details of primary members.  

Approval que details modal fix the receipt print layout. its overflowing in the print render. It should be contained in a single page. exactly like cash management receipt.

In Financial audit log for each transaction with attatched receipt show the receipt in the details modal.


Activity list entry should show primary members details in details modal.








